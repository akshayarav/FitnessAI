import React from 'react';

const GenderMenu=() => {
    return (
        <div>
            <h1>
                Gender:
            </h1>
            <form>
                <input id = "gender" type = "text" placeholder="Any"></input>
            </form>
        </div>

    )
}

export default GenderMenu